export const environment = {
  production: false,
  baseUrl: '/api/',
  hubUrl: '/hub/notifications',
  stripePublicKey: '',
  // Get key from: https://console.cloud.google.com → APIs & Services → Credentials → Create API Key → restrict to Maps JavaScript API
  googleMapsApiKey: ''
};
